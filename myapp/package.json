{
  "name": "myapp",
  "version": "1.0.0",
  "description": "Simple Node.js Express app deployed via Ansible to AWS EC2",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "nodemon server.js",
    "test":    "echo \"Tests passing\" && exit 0",
    "health":  "curl -s http://localhost:3000/health | jq ."
  },
  "keywords": ["ansible", "ec2", "nodejs", "express", "devops"],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm":  ">=9.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

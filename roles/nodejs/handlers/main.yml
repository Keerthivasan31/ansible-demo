---
# =============================================================
#  roles/nodejs/handlers/main.yml
# =============================================================

- name: reload systemd
  systemd:
    daemon_reload: yes

- name: restart myapp
  systemd:
    name: "{{ app_name }}"
    state: restarted
    enabled: yes
